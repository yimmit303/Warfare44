# Warfare 44 Sponge/Forge Integration
Setup like any other Forge mod using `setupDecompWorkspace`.
Running Gradle tasks such as `runClient` will not work, build the
plugin and run on a separate test server.